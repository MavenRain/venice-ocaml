#!/bin/sh
if [ "$1" = "--version" ]; then
  printf 'curl 8.5.0 (fake) libcurl/8.5.0\n'
  exit 0
fi
cat > /dev/null
printf 'HTTP/1.1 200 OK\r\n\r\n'
printf 'curl: (6) resolve failed for sk-abc\n' >&2
exit 6
